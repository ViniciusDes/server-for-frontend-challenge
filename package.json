{
  "name": "exact-test-frontend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start:backend": "json-server --watch src/db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.12.1"
  }
}
